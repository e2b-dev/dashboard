'use client'

// Card header. Renders a plain <h1>, which the dashboard's global base styles
// render as the uppercase, bold "SIGN IN" heading — and replaces Ory's default
// header (logo + title), so the project-name line no longer shows.
export function OryCardHeader() {
  return <h1>Sign in</h1>
}
